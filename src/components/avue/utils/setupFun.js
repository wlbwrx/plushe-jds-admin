import { toRefs } from 'vue';

export default (in_props, content) => {
  console.log('setupsetupsetup', props);
  const { modelValue, defaultValue, Dict, remoteFun, props, key, editType } = toRefs(in_props);
};
